{
    "name": "root",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "start": "lerna run start --scope=base-app --stream"
    },
    "devDependencies": {
        "lerna": "^4.0.0"
    }
}
